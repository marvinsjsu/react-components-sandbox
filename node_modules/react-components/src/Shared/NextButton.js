import React from 'react';

import { styles } from '../../utils/styles';

const NextButton = ({ onClickExit, exitLabel, onClickNext, nextLabel, disabled = false }) => {

  return (
    <div
      style={{
        ...styles.bottomLine
      }}
    >
      <span
        style={{
          ...styles.statement
        }}
      >
        <button
          style={{
            ...styles.buttonLink
          }}
          onClick={onClickExit}
        >
          {exitLabel}
        </button>
      </span>
      <button
        style={{
          ...styles.button
        }}
        onClick={onClickNext}
        disabled={disabled}
      >
        {nextLabel}
      </button>
    </div>
  );
}

export default NextButton;
import React from 'react';
import QRCode from 'qrcode.react';

import { styles } from '../../utils/styles';

const MainSetup = (props) => {
  return (
    <div style={styles.authInstructions}>
      <ol style={styles.instructionsList}>
        <li>1. Get an Authenticator App from the App Store.</li>
        <li>2. In the App select <span style={styles.bold}>Add/Setup Account.</span></li>
        <li>3. <span style={styles.bold}>Scan barcode.</span></li>
      </ol>

      <div style={styles.qrCodeContainer}>
        <QRCode
          size={110}
          value="http://presearch-token-marketplace.s3-website-us-west-1.amazonaws.com/login"
        />
        <br/>
        <a
          style={{
            ...styles.link
          }}
          href="#"
          onClick={props.onClick}
        >
          Can't scan it?
        </a>
      </div>
    </div>
  );
}

export default MainSetup;
import React from 'react';

import { styles } from '../../utils/styles';

const AuthySetup = (props) => {
  return (
    <div style={styles.authInstructions}>
      <p style={styles.instructionStatement}>
        Instructions for Authy Authenticator App
      </p>
      <ol style={styles.instructionsList}>
        <li>1. Tap the <span style={styles.bold}>plus symbol (+) / Add Account.</span></li>
        <li>2. Tap <span style={styles.bold}>Enter key manually.</span></li>
        <li>3. <span style={styles.bold}>Enter the key</span> in the <span style={styles.bold}>Adding new Account</span> dialog.</li>
        <li>4. Click <span style={styles.bold}>OK</span> to finish.</li>
      </ol>
      <br/>

      <a
        style={{
          ...styles.link
        }}
        href="#"
        onClick={props.onClick}
      >
        View instructions for Google Authenticator
      </a>
    </div>
  );
}

export default AuthySetup;